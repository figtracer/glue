# Prepared funding

Use the [prepared-work mode of `glue init refill`](refill.md#prepared-work). Existing `ready` policies, commands and saved jobs remain compatible. Glue funds the shortfall and never submits the prepared work.
