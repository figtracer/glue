{
  "name": "glue",
  "version": "0.1.0",
  "private": true,
  "description": "Small agent services for funding and keeping wallets usable.",
  "bin": {
    "glue": "src/cli.mjs"
  },
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/cli.mjs && node --check src/worker.mjs && node --check src/io.mjs && node --check src/authorization.mjs && node --check src/wallet.mjs && node --check src/service.mjs && node --check src/scheduler.mjs",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt --check .",
    "lint": "oxlint --deny-warnings src test scripts",
    "deps:check": "node scripts/check-dependency-age.mjs",
    "ci": "npm run fmt:check && npm run lint && npm run check && npm test"
  },
  "dependencies": {
    "accounts": "^0.18.4",
    "mppx": "^0.9.2",
    "viem": "^2.56.3"
  },
  "devDependencies": {
    "oxfmt": "^0.66.0",
    "oxlint": "~1.81.0"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
