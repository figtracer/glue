{
  "name": "glue",
  "version": "0.1.0",
  "private": true,
  "description": "A small, bounded gas-maintenance worker for Glue and Tempo.",
  "bin": {
    "glue": "src/cli.mjs"
  },
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/cli.mjs && node --check src/worker.mjs && node --check src/io.mjs"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
